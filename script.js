
/* CREATE a private array with 9 empty spaces

- Function getBoard():
    - Return the current state of the array

- Function placeMarker(index, marker):
    - IF the space at [index] is empty:
        - Put the player's marker into that array index
        - Return TRUE
    ELSE:
        - Return FALSE

*/

